# better-reads

## Client Build Instructions

From the outer folder, better-reads:

```
cd client
npm install
npm start
```

## Server Build Instructions

From the outer folder, better-reads:

```
cd server
npm install
npm start
```
